Issue : if import requests in python fails (ImportError: No module named requests)

Solution: Install python-requestes in ubuntu
          sudo apt-get install python-requests
          
Info:
Python can call either GET / POST nodejs API. Data can be sent from python to NodeJS API
